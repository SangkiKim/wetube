extends layouts/index.pug
include mixins/movieBlock

block content
    .movies__header
        h1 Movies!
    .movies
        each item in movies
           +movieBlock({
               id : item.id,
               title:item.title
            })

