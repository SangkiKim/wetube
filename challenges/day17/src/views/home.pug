extends layouts/main.pug
include mixins/videoBlock

block content
    p #{pageTitle} 
    .videos
        each item in videos
           +videoBlock({
               id:item.id,
               title:item.title
           })
