doctype html
html
        head
            title TXT2HTML
        body
            main
                h1 TXT2HTML!
                form(action=`/read`, method="post", enctype="multipart/form-data")
                  input(type="file", id="file", name="textFile",required=true, accept="text/*")
                  input(type="submit", value="Convert!")

